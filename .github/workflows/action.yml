name: hello world
 
on:
  push:
    branches: [ main ]
 
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
    - name: Hello world
      uses: actions/hello-world-javascript-action@v1
      with:
        who-to-greet: 'world'
